{
  "name": "pi-agent-swarm",
  "version": "0.0.0",
  "private": true,
  "description": "Experiment: Pi edits an Effect VFS workspace and tests committed revisions in Firecracker",
  "type": "module",
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=26.0.0"
  },
  "scripts": {
    "build": "tsc -b src",
    "check": "tsc -b",
    "clean": "tsc -b --clean && rm -rf dist dist-test",
    "demo": "pnpm build && node dist/main.js",
    "demo:cluster": "bash scripts/run-cluster-demo",
    "prepare:firecracker": "bash scripts/prepare-firecracker",
    "reset:fixture": "bash scripts/reset-fixture",
    "test": "vitest run"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "0.86.1",
    "@earendil-works/pi-coding-agent": "0.86.1",
    "@effect-vfs/core": "0.4.0",
    "@effect/platform-node": "4.0.0-rc.116",
    "@effect/platform-node-shared": "4.0.0-rc.116",
    "@effect/sql-pg": "4.0.0-rc.116",
    "@jpowersdev/effect-pi": "0.1.0",
    "effect": "4.0.0-rc.116"
  },
  "devDependencies": {
    "@effect/vitest": "4.0.0-rc.116",
    "@types/node": "26.4.1",
    "typescript": "7.0.2",
    "vitest": "5.0.1"
  }
}
