# Swarm fixture

A deliberately failing JavaScript project used by the `pi-agent-swarm` experiment. `pnpm setup:fixture` copies this template to `.data/fixture` and initializes the copy as a disposable Git repository.
